global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'cluster', 'service']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  receiver: 'default'
  routes:
    - match:
        severity: critical
      receiver: 'critical'
      continue: true

receivers:
  - name: 'default'
    webhook_configs:
      - url: '{{ alertmanager_webhook_url | default("http://localhost:5001/") }}'
        send_resolved: true

  - name: 'critical'
    webhook_configs:
      - url: '{{ alertmanager_critical_webhook_url | default("http://localhost:5001/critical") }}'
        send_resolved: true

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'cluster', 'service']
